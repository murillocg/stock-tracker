# Base URL of the read API. `terraform output api_url` prints it.
# Copy to .env.local (gitignored) — Vite only exposes VITE_-prefixed vars.
VITE_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/
